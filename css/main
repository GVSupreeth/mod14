/* ---------- DARK MODE EDGE FIX ---------- */

.sigma-edge {
    stroke: rgba(200, 200, 200, 0.4) !important;
    stroke-width: 0.8px !important;
}

.sigma-edge-hover {
    stroke: rgba(255, 255, 255, 0.8) !important;  /* bright on hover */
    stroke-width: 1.5px !important;
}

.sigma-edge-label {
    fill: #aaa !important;                        /* dim labels if any */
}
